[build]
builder = "python"

[build.env]
PYTHON_VERSION = "3.10"

[build.cmd]
install = "pip install -r requirements.txt"

[start]
cmd = "python main.py"

[deploy]
env = {
    MAX_PRODUCTS = "100",
    MAX_WORKERS = "24",
    MAX_THREADS_PER_WORKER = "200"
}
