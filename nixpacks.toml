[build]
builder = "python"

[build.env]
PYTHON_VERSION = "3.10"
PORT = "8000"
MAX_PRODUCTS = "100"
MAX_WORKERS = "24"
MAX_THREADS_PER_WORKER = "200"

[build.cmd]
install = "pip install -r requirements.txt"

[start]
cmd = "python main.py"
